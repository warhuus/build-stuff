# Phase 3 review — file structure and naming (instructions §12 phase 3 item 4)

Scope: §4 tree, §3 scaffolding, §10 limits/naming/JSDoc/readonly, §14 structural acceptance checks, and whether the structure/layering tests are sound. Report only; no project file was edited.
Scratch scripts: `/tmp/claude-0/-home-user-build-stuff/6668890e-cf53-523c-8931-07c0cb904c0d/scratchpad/{jsdoc.cjs,jsdoc2.cjs,fnlen.cjs,fnlen2.cjs,rettype.cjs,tsconfig.strict.json}`.
Known items L1, L2 (toDateOnly copy), L5, L6 (two loader envelope helpers) are not re-reported.

## Findings

| id | severity | file:line | finding | evidence | reference | suggested fix |
|---|---|---|---|---|---|---|
| STR-01 | major | src/data/metrics/source/osdk/paging.ts:69 (also source/fake/fakePaging.ts:64) | The host helper `chunk` is re-created as `chunkIds`. §3 says the code must import the host's `src/lib/osdk.ts` `chunk`, not re-create it. Nothing in `src/data` imports `src/lib/osdk.ts`. | `grep -rn "lib/osdk" src/data` finds nothing. `osdkFetches.ts:70,101` call `chunkIds(unique, ID_BATCH)`. | instr. §3 "Existing modules in the host app that you must import, not re-create" | In `source/osdk`, `import { chunk } from "../../../../lib/osdk"` and pass `[...ids]`, because `chunk` takes a mutable `T[]`. Delete `chunkIds` from paging.ts. §5.2 does not restrict `lib/osdk`, so the fake may import it too. List the dependency in INTEGRATION.md. |
| STR-02 | major | src/data/metrics/__tests__/structure.test.ts (missing check) | No test checks the §14 item "Every caveat code used anywhere has text in `CAVEAT_TEXT`". The type `Readonly<Record<Caveat,string>>` makes it complete at compile time, but §14 says "Checked by a test". | `grep -rln CAVEAT_TEXT src/data/metrics/__tests__` → only `index.test.ts`, which checks the export list. | instr. §14 acceptance, item 8 | Add a test that `Object.keys(CAVEAT_TEXT)` equals `CAVEATS` as a set and that every text is a non-empty string. Optionally scan production code for kebab-case string literals inside `caveats`/`caveatsIf(...)` arrays and assert each one is in `CAVEATS`. |
| STR-03 | minor | src/data/metrics/__tests__/structure.test.ts:24-25, 55, 79; layering.test.ts:40-43 | Gaps in the scan tests. (a) The `any` regex misses `Record<string, any>`, `Map<K,any>`, `type X = any`, `T = any`, `\| any`. (b) The default-export check misses `export { x as default }`. (c) The client-import check misses `"../../client.js"` and `"../client/index"`. (d) `localImports` maps `"./x.js"` to `x.js.ts`, which has rank -1 and passes silently. Any unranked import target (rank -1, e.g. `lib/osdk`) is never flagged. What already works: `import type`, `export … from`, `export *`, multi-line imports, dynamic `import()`, `typeof import()` and side-effect imports are all caught, and eslint `no-explicit-any` (typescript-eslint recommended) covers (a). | Ran the test's own regexes on samples: `import type` → caught, `export {x} from "@app/sdk"` → caught, `export * from` → caught, `"../../client.js"` → **missed**, `export { x as default }` → **missed**, `Record<string, any>` → **missed**. | instr. §10, §14; D22 | Match `any` with the TS AST (`SyntaxKind.AnyKeyword`) instead of a regex. Detect default exports with `ts.isExportAssignment` / an export specifier named `default`. Strip `.js`/`/index` before the client check and the rank lookup. In layering, fail when an import target has rank -1, apart from an explicit allow-list (`lib/osdk` from source/osdk). |
| STR-04 | minor | src/data/metrics/compute/funnel.ts:54; query/build.ts (`openItemsInWindow`); compute/stats.ts:19 | Names that §4 gives for mandated files are not used: `deriveFunnel` (funnel.ts exports `deriveStages`/`funnelSeries`), `itemsOpenInWindow` (build.ts exports `openItemsInWindow`), and `percent` (stats.ts exports `fraction`). DESIGN.md used the new names, but no decision records the rename. | `grep -rn "export .*deriveFunnel\|itemsOpenInWindow\|percent"` → none. | instr. §4 (compute/funnel.ts, query/build.ts, compute/stats.ts), §10 naming | Rename to the §4 names, or record the rename in REVIEW.md. `fraction` fits the 0..1 rule better, so recording it is enough. |
| STR-05 | minor | query/buildRisk.ts:26 vs source/osdk/compileWhere.ts:113; shared/concurrency.ts:109 vs source/osdk/paging.ts:81; shared/errors.ts:20,29 vs source/osdk/paging.ts:21,26 vs source/fake/fakePaging.ts:26; source/fake/fakePaging.ts:64 vs source/osdk/paging.ts:69; compute/otifOutcome.ts:30 vs source/fake/fakeGroups.ts:75; compute/dimValues.ts:33 vs source/osdk/groupBy.ts:157 | Different exported symbols share the same name but have different contracts. `riskWhere` is a spec builder (`(set, cond) → RiskSet`) in one file and a where-clause compiler (`(cond, config) → WhereClause`) in the other. `runLimited` has two signatures (`tasks[]` vs `inputs + task`). `abortError` builds two different messages. `throwIfAborted` exists three times (optional vs required signal). `chunkIds` exists twice (floor vs no floor). `inVerdictWindow` and `escalatedLabel` exist twice each. A reader or auto-import can easily pick the wrong one. | Script over all 592 exported symbols, duplicate names listed. `toDateOnly` is excluded (L2). | instr. §10 "Name things for what they are"; §5 (reuse) | Rename the compiler to `compileRiskCondition`. The fake should import `inVerdictWindow` from compute (D8 allows it). The osdk `escalatedLabel` should delegate to or reuse the compute one after the `String(v)` normalisation (F6), or be renamed `escalatedGroupLabel`. Move `abortError`/`throwIfAborted`/`runLimited` into one module at or below the port (for example a new `source/abort.ts`, rank 3) that shared/, osdk/ and fake/ all import. |
| STR-06 | minor | compute/dimValues.ts, compute/deriveCommon.ts, shared/inflight.ts, shared/sourceCtx.ts, source/osdk/defaultSource.ts, source/fake/fakeGroups.ts, fakePaging.ts, fakeTypes.ts, fixtureAlerts.ts | §4 allows extra files only when a file would exceed the size limits. D21 and DESIGN justify most extra files, but not these. The fake split is justified by size (683 lines in total) but is not listed in D21. `fixtureAlerts.ts` is what D21 calls `fixtureEvents.ts`. `inflight.ts` + `memo.ts` = 166 lines, so size does not justify that split (reuse by cache.ts does). `defaultSource.ts` exists because of §5.2 (hooks may not import client). | `wc -l`; PHASE1_DECISIONS D2/D7/D8/D9/D21; DESIGN "Compute inventory". | instr. §4 "You may add files … when a file would exceed the size limits" | No code change. Add a row per file to REVIEW.md with the reason (reuse across cards, D7/D9; §5.2 boundary for defaultSource; size for the fake split) so the §4 deviation is on record. |
| STR-07 | minor | src/data/metrics/catalogue.ts:74, 81 | The stub cards' loader (`loadStub`) and derive (`deriveStubRows`) are defined in catalogue.ts. §4 puts loaders in `loaders/` (with `blocked.ts` for blocked cards) and every derive in `compute/`. catalogue.ts is meant only to pair them. | Source lines 69-83. `loaders/blocked.ts` has only `stubBlocked`/`integrationBlock`. | instr. §4 (catalogue.ts, loaders/, compute/derive*.ts), §5.3 | Move `loadStub` to `loaders/blocked.ts` and `deriveStubRows` to a `compute/` derive file, for example `deriveCommon.ts`. The catalogue then only pairs them. |
| STR-08 | minor | src/data/metrics/__tests__/source/osdk/rowMapping.test.ts:12; aggregates.test.ts; fetches.test.ts; __tests__/shared/deferred.ts, loaderDeps.ts | The test tree does not fully mirror the source tree. The tests for the mandated `source/osdk/paging.ts` (row cap, abort between pages) are in `rowMapping.test.ts`. `osdkAggregates.ts` and `osdkFetches.ts` are tested by `aggregates.test.ts` and `fetches.test.ts`. There is no `paging.test.ts`, `osdkSource.test.ts` or `groupBy.test.ts` (coverage is still 100% through other tests). The generic test helpers `deferred.ts` and `loaderDeps.ts` are in `__tests__/shared/`, which suggests they mirror `shared/` sources that do not exist. | Mirror script: 19 production files have no same-named test (7 of them are type-only or frozen files). The paging import is in rowMapping.test.ts:12. | instr. §4 `__tests__/ Mirrors the tree`; §11 | Move the paging cases to `__tests__/source/osdk/paging.test.ts`. Rename `aggregates.test.ts` → `osdkAggregates.test.ts` and `fetches.test.ts` → `osdkFetches.test.ts`. Move the helpers into a `__tests__/helpers/` (or `testUtils/`) folder. |
| STR-09 | minor | tsconfig.json:3-17 | §3 says "Match it exactly", but tsconfig adds `types: ["vitest/globals", "node"]`, `lib`, `module`, `skipLibCheck`, `esModuleInterop` and `resolveJsonModule`. `vitest/globals` declares `describe`/`it` globals, yet vitest.config.ts does not set `globals: true`, so a test that forgets to import from vitest would type-check and then fail at runtime. `node` exposes Node types to delivered `src/`. | Checked: delivered src (without `__tests__`) type-checks cleanly with the exact §3 options, `types: []` and `skipLibCheck: false` (scratchpad/tsconfig.strict.json). Every test file imports from `vitest`. There is no latent dependency today. | instr. §3 Language row | Drop `vitest/globals` (unused). Scope `node` to tests, through a `tsconfig.test.json` or a `/// <reference types="node" />` in the two scan tests. Keep `lib`/`module`/`skipLibCheck` and list them in README as standalone-only. |
| STR-10 | minor | compute/deriveUserFunnel.ts:87; compute/deriveItemFunnel.ts:15; compute/deriveItemFunnelAlert.ts:174; compute/deriveItemFunnelItem.ts:83; compute/deriveRiskDistribution.ts:67; compute/deriveClosureComposition.ts:18; compute/otifOutcome.ts:21; query/build.ts:46; source/fake/fakeGroups.ts:75; shared/cache.ts:93 | Every exported symbol has a JSDoc (592 of 592). But several business-rule exports cite only a card number ("3.1 derive", "section 2"), or rely on the file header, instead of a spec reference (`Spec §9 …`). Two docs leave out null or optional behaviour: `fakeGroups.inVerdictWindow` does not say that a null date returns false, and the `getOrLoad` `@param signal` does not say what happens when the signal is absent. | jsdoc.cjs / jsdoc2.cjs: 0 exports without JSDoc; 3 exports with a nullable signature whose doc does not mention null; 43 compute/loader/query exports without a § citation. Most are generic helpers (sum, intersectIds), which is fine; the ones listed here are business rules. | instr. §10 "Every exported symbol has a JSDoc … Business rules cite the spec" | Add `Spec §9 <row>` to the six derive docs, to `verdictOf` (§9 4.1) and to `eventsWhere` (§9.0 window rule "now = ≤ end"). Add the null/absent behaviour to the two docs named. |

## Verified OK (no finding)
- **§4 tree:** every mandated file exists at its mandated path: index, types, selection, window, breakdowns, catalogue, loadCard; compute/{funnel, alertLifecycle, riskBuckets, bins, durations, ageing, otifOutcome, composition, stats, breakdown, derive*}; query/{specs, build}; source/MetricsSource; osdk/{osdkSource, compileSpec, paging}; fake/{fakeSource, fixtures}; shared/{touchedAlerts, cache, concurrency, errors}; the ten loaders; the three hooks.
  - No mandated file is renamed.
  - No new top-level folder: `src/` holds config, data, lib, client.ts.
  - No extra sub-folder under data/metrics.
  - Every added file sits inside an allowed folder.
- **Package, compiler and tool config:**
  - package.json has every §3/X6 dependency with the right range: typescript ~5.5 (installed 5.5.4), vitest ^3.2.7, @vitest/coverage-v8, jsdom, @osdk/client ~2.7 and @osdk/api ~2.7 (both 2.7.8), react / react-dom / @types ^18, react-router-dom ^6, @testing-library/react, eslint ^9, typescript-eslint, eslint-plugin-react-hooks. The extras @eslint/js and @types/node are needed by the eslint config and the scan tests.
  - tsconfig has every required option, `paths` for `@app/sdk`, and include `["src","stubs"]`.
  - vitest.config.ts has the same alias and `environment: "jsdom"`.
  - eslint.config.mjs uses typescript-eslint recommended plus react-hooks. `npx eslint . --max-warnings 0` exits 0.
- **Delivery and stubs:**
  - `.deliveryignore` lists exactly `src/client.ts`, `src/lib/osdk.ts`, `stubs/`.
  - `stubs/app-sdk/index.ts` uses `@osdk/api` types, with no `any` and no casts.
  - `src/client.ts` exports a typed `Client` as a named and a default export; every request rejects. That default export is mandated by §3.
  - `src/lib/osdk.ts` holds the two host helpers.
- **§10 limits:**
  - Largest src file: selection.ts at 221 lines (the stubs file, 399 lines, is outside src).
  - Largest loader: itemFunnelItemView.ts at 145 lines.
  - Longest production function: shared/memo.ts:36 at 40 lines, measured independently from the `const`/`export` line.
- **The size-limit checks are sound:**
  - `ts.isFunctionLike` + body covers function declarations and expressions, arrow functions assigned to consts or properties, object and class methods, constructors and accessors.
  - An arrow function is measured from its `(`, which is usually on the `const` line, so the error is 0–1 line at most.
  - The 250-line check scans all of `src/`, including tests and stubs.
- **The import scan is sound for:** `import type`, `export … from`, `export *`, dynamic `import()`, `typeof import()`, multi-line imports and side-effect imports (gaps are in STR-03).
- **Naming and exports:**
  - The loader and derive conventions hold: `load<CardId>` / `derive<CardId>` for all nine cards.
  - No positional names like `set3`; only short-lived locals such as `s`/`result`.
  - Named exports only; the one default export is the mandated stub in client.ts.
- **Clean code:** no TODO, `console.*`, commented-out code or `eslint-disable` (except three `react-hooks/exhaustive-deps` disables, each with a reason). Also no `as unknown as`, `@ts-ignore` or `any`.
- **Readonly:** config and catalogue data are readonly: tuples are `as const`, `Readonly<Record<…>>` is used, `MetricsConfig`/`CardMeta`/`StubMeta`/`BreakdownRule`/`Selection`/`ItemFilters` have readonly fields, and the `BREAKDOWN_REGISTRY` and `CARD_IMPL` types are readonly.
- **Explicit types:** every exported function has an explicit return or declared type (checked by script; the only exception is a fixture helper whose default parameter is inferred).

## Notes
- `README.md`, `INTEGRATION.md`, `QUESTIONS.md` and `REVIEW.md` are not in `metrics-data-layer/` yet. They are phase-4 deliverables (§14), so they are not reported as findings.
- §4 says catalogue.ts is "imported by loadCard.ts only", but index.ts also imports it (`export { CARDS } from "./catalogue"`). §7 of the instructions prescribes exactly this line, so this is consistent with the binding API and not a finding.
- Out of 7 runs, my first run of `structure.test.ts` + `layering.test.ts` had 1 failing test. The failure did not come back in 6 reruns, and git status is clean. It was most likely a temporary file that another parallel agent created under `src/` (the scans walk the whole tree). Phase 4 should run these tests on a quiet tree.
- Out of my dimension, seen in passing: `osdkFetches.ts` `verdictsByEq` calls `fetchPage` directly rather than through `paging.ts`, which conflicts with §5.6 "Every row fetch goes through paging.ts". This is for the OSDK reviewer.

## Summary
- Blockers: none.
- Majors:
  - STR-01: host `chunk` from `src/lib/osdk.ts` is re-implemented as `chunkIds` instead of being imported (§3).
  - STR-02: no test for the §14 acceptance item "every caveat code has text in CAVEAT_TEXT".
- Minors STR-03…STR-10: scan-test gaps, §4 names not used, same-named exports with different contracts, added files that need a recorded reason, stub loader/derive placed in catalogue.ts, test-tree mirror mismatches, extra tsconfig types, a few JSDoc docs missing spec citations or null behaviour.
