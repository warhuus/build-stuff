# Phase 4 — fixer G4 (tests: src/data/metrics/__tests__/** only)

No production file was changed. Paths below are relative to `metrics-data-layer/src/data/metrics/__tests__/`.
Final checks (whole repo, 2026-09-25): `npx tsc --noEmit` clean · `npx eslint . --max-warnings 0` clean ·
`npx vitest run --coverage`: **89 files, 798 tests, all green** (was 78 / 698).

## Coverage (`npx vitest run --coverage`)

| Scope | Statements | Branches | Functions | Lines |
|---|---|---|---|---|
| All included files | 100% (4629/4629) | 99.15% (1867/1883) | 100% (566/566) | 100% (4629/4629) |
| `compute/` | 100% (1420/1420) | 98.81% (664/672) | 100% (182/182) | 100% (1420/1420) |

Acceptance (100% lines on compute/, ≥ 90% overall) is met. Before my never-arm tests, compute/ lines were 99.65%
(G1's new `unhandledDimension` default arms in `dimValues.ts:28,65,118,147` and `deriveItemFunnelAlert.ts:201`).
Remaining uncovered branches (lines are 100%): `deriveItemFunnelAlert.ts` `?? []` fallbacks for null
`facts`/`openAlerts` (unreachable since COR-01: the loader always fills them), `deriveCommon.ts:53`,
`userFunnel.ts:66,71` (see "Production notes"), `selection.ts:55`, `MetricsSourceContext.ts:47`, `fakeEval.ts:53`,
`compileWhere.ts:131`, `paging.ts:40,86` (defensive guards).

## Per finding

| id | what changed |
|---|---|
| TST-01 / STR-02 | New `config.test.ts`: CAVEAT_TEXT keys = CAVEATS, each text non-empty and a sentence; every BlockedReason has text; INTEGRATION_UNBLOCKED_BY non-empty for every INTEGRATION_KEY; the three stubs (3.2, 3.3, 4.7) have reason, non-empty `unblockedBy`, known caveats, and `CARDS` agrees. §14 scan: (a) type-checker scan (a `ts.Program` over every production file) of every string literal whose contextual type is a caveat-code union (all literal members ∈ CAVEATS) — each must be a CAVEAT_TEXT key, with anti-vacuity (≥ 25 distinct codes incl. row-cap, truncated, overlap, not-worked-window-cap, value-item-view-only, build-stamp); (b) a syntactic net: kebab-case literals inside a `caveats` property/variable or a `*caveat*` call must be keys. No unknown code found. |
| TST-02 / OSD-03 | New `source/osdk/planChains.test.ts` (L1, L2 evSet, L2 openIds, L3 ×3, 4.2 not-worked pair, 4.6 closedTotal ± group-by, 3.1 soeAll/soeWorked/byRange/bucket value/delayed/item-dim split, 1.2–1.4 incl. escalated pivots, 1.1, 4.1 worked ids and gated totals for both modes and "now", itemsById) and `planChainsItemFunnel.test.ts` (so20–so24, both outside paths at 7 d and now, perGroup for escalated and persona, the three 2.1 alert-view terms), each with and without item filters. The REAL builders (`query/build.ts`, `buildFunnel.ts`, `buildRisk.ts`) are compiled through `createOsdkSource` over the recording client; the expected chains/where objects are written from the spec §9 pseudocode in the new `helpers/specPlans.ts` (PRED literals, `tsIn`, `events`, `aofSet`, `finalSet`, 2.0 `open`), never from production code. Also `$select` for the row fetches and `groupBy` fields. New `planChainsLoaders.test.ts`: every first-draft card loaded via `loadCard` against the recording client, request mix = spec cost lines (e.g. userFunnel 4 aggregates; item view 7; alert view now 1 aggregate + 4 row fetches; 3.1 20 aggregates; 4.2 7 d 5 row fetches, 30 d 3). All chains matched the spec; no deviation found. |
| TST-03 | New end-to-end fixture tests through `loadCard(createFakeSource, FIXTURE_CONFIG, FIXTURE_NOW)` with the working in comments: `loadCardDurations.test.ts` (4.2 7 d and 14 d: worked/not-worked bins, median, p90, noRaise, clampedNegative; 4.2 7 d priority groups incl. per-group quantiles and other; 4.3 30 d bins/median/p90; 4.4 7 d and 14 d incl. closeBeforeView = 1 for A53), `loadCardBacklog.test.ts` (4.5 N = 30: 21 non-empty alert bins, threshold tile 16 / 232 000 / 16⁄45, item bins; N = 10 from the cache without refetch; 4.6 7 d total and alertType / routingPersona / priority groups with other = 0; 14 d), `loadCardAlertView.test.ts` (alert view 7 d stages/pctPrev/pctFirst/trackValue/outside paths, now, unit valueUsd → count + value-item-view-only (SPF-01), alertType, routingPersona 7 d and now, escalated 7 d with closed alerts in other, actionType/writebackType overlap). COR-01/02/03 values re-derived by hand (2.2 = 12 without A53, outside 2.3 = 4 without A54; escalated 7 d other = [10, 3, 2, 0]; now routingPersona 2.2 Planner 11 / Logistics 9 / CustomerService 5). The three alert-view end-to-end tests moved here from `loaders/itemFunnelAlertView.test.ts`. Every value matched on the first run. |
| TST-04 | 14 d and 90 d added: `loaders/riskDistribution.test.ts` (worked 18 items / 253k; 22 items / 330k), `loaders/otifOutcome.test.ts` (both modes; totals, worked ids, verdict rows), `loaders/raisedToFirstView.test.ts` (L2(14), L2(90) ids), `loaders/firstViewToClosure.test.ts` (it.each 7/14/30/90/now), `loaders/closureComposition.test.ts` (closedTotal 14 at 14 d, 21 at 90 d). |
| TST-05 | `loaders/itemFunnelAlertView.test.ts`: the `-1` sentinel is gone (L1 rows 30 d = 47, 90 d = 62, with working); humanEvents, facts and openAlerts are asserted as id lists (`TOUCHED_IDS`, `OPEN_IDS`, now in `helpers/alertCards.ts` with the per-window working) instead of lengths; phase2-D2 citations replaced by working. Same for `raisedToFirstView.test.ts` (30 d list instead of length 34). |
| TST-06 | `planChainsItemFunnel.test.ts` compiles EventSet `intersect` and OpenAlertSet `union` / `subtract` (the compileSpec branch at :120 is now covered). |
| TST-07 / STR-03 / MOD-20 | `structure.test.ts` and `layering.test.ts` rewritten on a shared AST scan (`helpers/sourceTree.ts`: files read and parsed once; module specifiers from imports, re-exports, `import()`, `import type()`, `require`). `any` = AST `AnyKeyword` (generics, aliases, returns, casts; self-test). Default exports: `export default`, `export =`, `export { x as default }`, `export { default } from` (self-test). Client imports: `@osdk/*`, `@app/sdk`, and relative specifiers resolving to `src/client` with any of `.js`/`.ts`/`/index` (self-test). Layering: specifiers normalised (`.js`, `/index`), unranked targets (-1) fail except the allow-list `lib/osdk.ts` from `source/batching.ts`, `source/osdk/**`, `source/fake/**` and `client.ts` from `source/osdk/**`; `source/fake` ↔ `source/osdk` imports fail; `source/batching.ts` stays rank 3 (asserted); edge-rule self-test. Purity now covers the whole rank-2 layer (compute/, query/, window.ts, selection.ts, breakdowns.ts; asserted) and flags top-level `let`/`var` (module state) plus `performance.now(`. `@ts-nocheck` added to the directive check. |
| TST-08 | `loadCard.test.ts`: `r.data toBeDefined` replaced by one exact hand-derived headline per card (userFunnel [null,3,5,5,1], item view [31,29,11,7,1], 3.1 14 rows summing to 30, full 4.1 headline, 4.2–4.6 counts); caveat union asserted with `toEqual` in CAVEATS order; threshold 7 vs 30 exact (29 vs 16; A59 exactly 7.0 d is not > 7). `loadCardRuntime.test.ts`: final `progress` exactly `{ loaded: 268 }` for 4.2 at 7 d (66 + 149 + 31 + 9 + 13, working in the comment); `hooks/useMetric.test.ts` settled progress likewise. `source/fake/fixtures.test.ts`: events `toHaveLength(164)`. Kept as is: `useMetric.test.ts` "reports progress while loading" (`> 0` at the gate: the value depends on which parallel fetch finished first). |
| TST-09 / D20 | New `loadCardRowFetches.test.ts`: every first-draft card × window key × view × allowed breakdown (and none) × {no filter, AMER} (≈ 500 loads, > 1000 row calls) against the fake; every `fetchEvents`/`fetchOpenAlerts`/`fetchItems` call must equal one of the builder outputs (L1, L2 chain, L2 openIds at the selected window or now; L3 ×3; closedNotOpenNow and its opened events; workedItems); `fetchItemsByIds` only for 4.2–4.4 with an item dim; `fetchVerdictsByIds` only for 4.1. Plus: section 1, the item view and 3.1 make no row fetch. |
| TST-10 | `compute/deriveDurations.test.ts`: one loop over `allowedBreakdowns(card, "item")` (7 dims) for each of 4.2, 4.3, 4.4 with expected groups/other. `compute/deriveAgeingBacklog.test.ts`: loop over the 8 dims of 4.5 (incl. routingPersona, priority, escalated; overlap caveat only for alert dims). |
| TST-11 | Covered on fixture COPIES in `loadCardEdgeCases.test.ts` (fixture data unchanged): u1 viewing as Logistics too counts in both userFunnel queueFilter groups on 1.2; a first view before the raise stamp (4.3 clampedNegative 1, bin [0,1) 2); a closed alert whose op stamp is after its close stamp (4.2 worked 7, clampedNegative 1); A25 with two write-back types (writebackType groups 1 + 1, overlapRatio 2). |
| TST-12 | `loadCardRuntime.test.ts`: `computedAt` of the default-clock load is asserted between `Date.now()` read before and after the call (no dependence on the machine date). |
| MOD-19 (test-helper dup) | One `sel(overrides)` in `helpers/testKit.ts` replaces the 11 copies (loader tests now call `sel({ window, filters, view, otifMode })`); one `fakeCtx(over, onProgress?, signal?)` replaces the ctx builders of `fakePaging.test.ts`, `fakeSource.test.ts`, `query/buildFunnel.test.ts` (the OSDK harness keeps `makeCtx`: other config, progress log). One test source wrapper (`helpers/wrapSource.ts`). Not done (production file): the fake's own interceptor `source/fake/fakeSource.ts` `recording` duplicates `wrapSource`; merging them needs an exported `interceptSource` in production code (G2/lead). |
| STR-08 | Paging cases moved to new `source/osdk/paging.test.ts` (from `rowMapping.test.ts` and the paged-fetch describe of the fetch tests) plus unit cases for `fetchAllPages` (slice at the cap, abort before the first page), `fetchMapped`, `fetchFirstPage`. `aggregates.test.ts` → `osdkAggregates.test.ts`, `fetches.test.ts` → `osdkFetches.test.ts`. New `source/osdk/groupBy.test.ts` (finiteOrZero, aggregateRows, toGroupCounts, escalatedGroupLabel, riskByRanges). Helpers without a source moved to `__tests__/helpers/`: `deferred.ts`, `loaderDeps.ts` (from shared/), `deriveRows.ts` (was compute/deriveTestUtils.ts), `alertCards.ts` (was loaders/alertCardTestUtils.ts), `wrapSource.ts` (was loadCardTestUtils.ts), `osdkHarness.ts` and `recordingClient.ts` (from source/osdk/); new `testKit.ts`, `sourceTree.ts`, `specPlans.ts`. Remaining non-mirroring test names are deliberate splits of one source's tests: `loadCard*.test.ts`, `loaders/itemFunnelItemAlertDims.test.ts`, `loaders/alertCardsEndToEnd.test.ts`, `shared/sharedLoaders.test.ts`, `source/osdk/groupByOnly.test.ts` (D16), `source/osdk/planChains*.test.ts`. |
| ≤ 250 lines | Every test file ≤ 250 (largest: `helpers/recordingClient.ts` 228, `hooks/useMetric.test.ts` 227). |
| never arms | `compute/dimValues.test.ts`, `breakdowns.test.ts`, `compute/deriveItemFunnelAlert.test.ts` feed a dimension parsed from wire JSON (`testKit.fromWire`, no cast) to every exhaustive switch: each throws TypeError. `loaders/itemFunnelItemView.test.ts`: an off-registry dim rejects with RangeError. |

## Production notes (no fix; evidence)
- `loaders/userFunnel.ts:66` and `:71` (`RangeError` for a dim at 1.1 other than queueFilter, and for a dim without an AlertHistory field) are unreachable through `loadUserFunnel`: `allStageGroups` first keeps only `stagesForDim("userFunnel", "item", dim)`, which is empty for any dim outside the registry row, so `loadUserFunnel(sel, "plant", deps)` resolves with `groups: {}` instead of rejecting (checked with a test, then removed). Harmless (loadCard rejects such dims first, `breakdown-not-allowed`), but the TYP-05 comment overstates it. For G1/REVIEW.
- `compute/deriveItemFunnelAlert.ts` `raw.facts ?? []` / `raw.openAlerts ?? []` are dead since COR-01 (the loader always fills both); matches G1's open interface change request on the `AlertViewRaw` doc.
- `source/fake/fakeSource.ts:156` `recording` duplicates the test `wrapSource` (MOD-19, production side).
- No numeric or plan deviation found: every hand-derived expectation (≈ 150 values) and every spec §9 chain matched the code on the first run.

## Notes for the lead
- The scan tests (`structure`, `layering`, `config`) use `node:fs`/`typescript` and need Node types in the test compile (STR-09: keep `node` available to tests if tsconfig `types` changes). `config.test.ts` builds a `ts.Program` (≈ 1.5 s).
- Lead commit 32d7f80 captured this work mid-way; the tree now contains the finished state (uncommitted).

## QUESTIONS.md entries (for G5)
None new. (TST-11 edge cases are covered by fixture copies inside the tests, so the fixture dataset and its documented counts are unchanged; REVIEW.md may note it.)

## Interface change requests
None.
