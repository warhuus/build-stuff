# Phase 4 — fixer G1 (compute, query, loaders, catalogue, breakdowns, window, selection.hasFilters)

Checks at the end (whole repo, with G2/G3 work present): `npx tsc --noEmit` clean, `npx eslint . --max-warnings 0` clean, `npx vitest run` 78 files / 698 tests pass.
Paths below are relative to `metrics-data-layer/src/data/metrics/`.

## Per finding

| id | what changed |
|---|---|
| COR-01 | `compute/deriveItemFunnelAlert.ts`: new exported `carriedPopulation(raw)` = L3 open-now ids ∪ (bounded window) L2 facts with `closedAt` in the window (`inWindow`, start inclusive); under "now" open-now only. The total's sets, every additive group's sets and both outside paths are computed over this population only. `loaders/itemFunnelAlertView.ts` always loads `loadTouchedAlerts(selected w, f)` and `loadOpenAlerts(f)` (both memoised; L3 shared with 4.5), so `facts` and `openAlerts` are always filled (types unchanged). The `optional()` helper and the "now-only" restriction were removed. |
| COR-02 | `deriveItemFunnelAlert.ts` `alertKeyOf`: an alert open now takes its group from its L3 AOF row (`openAlertDimValue`) on every stage; a closed alert from the L2 `attrs`; escalated only for open alerts (closed → no group → `other`). Groups are built over the 2.1 population (`alertIdsByGroup(population, keyOf)`). |
| COR-03 | `compute/breakdown.ts` new `nonEmptyGroups`. Used in `deriveItemFunnelItem` (ranking), `deriveUserFunnel` (ranking; covers the escalated true/false pair), `deriveItemFunnelAlert.carriedRanking` (replaces its inline filter) and `loaders/itemFunnelItemView.alertDimGroups` (top-N for 2.2–2.4 chosen from non-empty candidates only, so empty groups are not queried). The raw 2.1 list keeps every candidate (see MOD-02). |
| SPF-01 | `compute/funnel.ts` `funnelSeries`: `unit` = `"count"` when `usesCountFallback`, else the selected unit; `value-item-view-only` unchanged. Tests updated (`funnel.test.ts`, `deriveUserFunnel.test.ts`, alert-view test). |
| SPF-06 | `deriveUserFunnel.ts` `groupedCallLists`: the `MAX_GROUPS` check skips the escalated lists (two ungrouped counts). Test with `MAX_GROUPS: 2`. |
| MOD-02 | `truncated` (MAX_GROUPS) is now decided only in derives. Loaders no longer pass grouped lists or config to an envelope. `deriveOtifOutcome` now adds `truncationCaveats(null, [raw.totals], config)`. `deriveItemFunnelItem.groupedCallLists`: item dims → every stage's `countItemsBy` list; alert dims → the 2.1 list only (one entry per candidate of the grouped `countOpenAlertsBy` call; 2.2–2.4 lists are ungrouped per-group counts). Loader tests that asserted a loader-side `truncated` now assert the loader gives no caveat and the derive (with the same config) adds it. |
| MOD-04 | `breakdowns.ts` exports `ALERT_ATTR_DIMS`, `AlertAttrDim`, `isAlertAttrDim`, `ALERT_DIMS`, `isOpenAlertFilterDim` (narrows to specs' `OpenAlertFilterField`), `eventGroupFieldOf` (exhaustive), `unhandledDimension` (never arm), `QUERIED_USER_STAGES`, `isQueriedUserStage`, `ALERT_VIEW_STAGES`. Deleted local copies: `itemFunnelAlertView.isAttrDim/AttrDim`, `itemFunnelItemView.ItemViewAlertDim/isItemViewAlertDim`, `closureComposition.eventGroupOf`, `userFunnel.eventGroupField`, `riskDistribution.itemDimOf` (now `isItemDim`). |
| MOD minors | MOD-05: `compute/dimValues.ts` `escalatedFlagLabel` (one label rule; `escalatedLabel` delegates) + `escalatedValueOf` (reverse); userFunnel and item-view `conditionOf` use them. MOD-06: `alertLifecycle.groupEventsByAlert` deleted, `alertFactsForIds` uses `groupRows`. MOD-07: `query/build.hasItemFilters` deleted; `build.ts`/`buildRisk.ts` use `selection.hasFilters`. MOD-08: `compareCodeUnits` + `sortedDistinct` in `compute/stats.ts`; `compareGroupCounts` uses it; `loaders/otifOutcome.ts` imports from compute (the shared copy was removed in the G2 WIP commit). MOD-09: stage lists from `FUNNEL_STAGES` (breakdowns registry, userFunnel loader, deriveUserFunnel, item-view loader, alert-view derive). MOD-10: `riskDistribution` loader maps `[...SCORED_BUCKETS, "delayed"]`; `riskBuckets.fromEntries` keeps its literal record on purpose (TS-checked: a new bucket fails to compile there; building it from the tuple would need a cast). MOD-12 (cheap part): `additiveRowBreakdown` and `compositionBreakdown` reuse the `groupRows` map instead of re-filtering. MOD-13: `durations`, `ageing`, `bins`, `breakdown` use `clampNonNegative`/`sumBy`; `otifOutcome.inVerdictWindow` uses new `window.dateOnlyOrNull` (no magic 10). MOD-18: deleted `stats.sumNullable`, `riskBuckets.emptyBucketAmounts` (test got a local helper), `OutcomeComputation.clamped` (`outcomeHeadline` now returns `OutcomeHeadline`). Not done (not cheap / not mine): MOD-11 funnel caveat assembly, MOD-12 `amountsOutside`, the `"2.1": other("2.1")…` record in `deriveItemFunnelAlert.additiveSpec` (typed record, compile-checked), MOD-17 key labels (selection.ts beyond hasFilters is not mine). |
| L5 | `loaders/alertCardWiring.ts` `loadFactsDuration(input, deps)` takes a `populationOf` (the pure compute population function) and fetches items only for the population; `raisedToClosed` uses `raisedToClosedPopulation`, `raisedToFirstView` `raisedToFirstViewPopulation`, `firstViewToClosure` `firstViewToClosurePopulation`. Excluded alerts (noRaise, closeBeforeView) stay in the population (breakdowns rank over them). |
| L6 | New `loaders/loaderOutput.ts` `loaderOutput(raw, fetches = [], flags = {})`: only `row-cap` and `not-worked-window-cap`. Deleted `loaders/funnelLoaderOutput.ts` and `alertCardWiring.loaderEnvelope`; every loader uses `loaderOutput`. Envelope tests moved to `__tests__/loaders/loaderOutput.test.ts`. |
| OSD-02 | `itemFunnelItemView.perGroup` runs the per-candidate `countItems` through `shared/concurrency.runLimited(tasks, config.INNER_CONCURRENCY, signal)`. |
| STR-04 | `deriveStages` → `deriveFunnel` (compute/funnel.ts), `openItemsInWindow` → `itemsOpenInWindow` (query/build.ts, buildFunnel.ts), `fraction` → `percent` (compute/stats.ts, ageing.ts); tests updated. |
| STR-05 (my part) | No remaining same-name/different-contract pair in my files: `sortedDistinct` exists once (compute/stats); `inVerdictWindow` once (compute; fake imports it); `escalatedLabel` in compute keeps its signature (the OSDK helper is G2's rename). `query/buildRisk.riskWhere` keeps its name (G2 renames the OSDK compiler). |
| STR-07 | `loadStub` → `loaders/blocked.ts`; `deriveStubRows` → new `compute/deriveStub.ts`; catalogue only pairs them; `catalogue.test.ts` imports updated. |
| STR-10 | Spec citations added: `deriveUserFunnel`, `deriveItemFunnel`, `deriveItemView`, `deriveAlertView`, `deriveRiskDistribution`, `deriveClosureComposition`, `deriveOtifOutcome`, `otifOutcome.verdictOf` (§9 4.1), `build.eventsWhere` (§9.0 `tsIn`, "now" = ≤ end). New exports all have JSDoc with null behaviour. |
| TYP-05 | Exhaustive switches with a `never` arm (`unhandledDimension`) at every `BreakdownDimension` dispatch in my files: `breakdowns.eventGroupFieldOf`, `dimValues.attrsDimValue` (param narrowed to `AlertAttrDim`), `openAlertDimValue` (param narrowed to `OpenAlertGroupField`), `factKeyOf`, new `openAlertKeyOf` (moved from deriveAgeingBacklog), `deriveItemFunnelAlert.alertViewBreakdown`. `isAlertDim` is now positive (`ALERT_DIMS`). userFunnel branches on `dim === "escalated"` explicitly, groups 1.1 only for queueFilter, and throws `RangeError` for a dim with no field; the item-view `groupsFor` and the alert-view dispatch throw `RangeError` for dims outside their registry row. Dims outside a registry row that key rows (`factKeyOf`/`openAlertKeyOf`) are listed explicitly and key nothing. |

## Fixture expectations re-derived by hand (working is in the test comments)
- `__tests__/loaders/itemFunnelAlertView.test.ts`: alert view 7 d = [null, 57, 12, 5, 1], outside 2.3 = 4, outside 2.4 = 0 (A53 cl@8 and A54 cl@13 leave 2.1). alertType groups LateGI [27, 8, 5, 1], CreditBlock [16, 2, 0, 0], Allocation [14, 2, 0, 0], other 0. routingPersona 2.2–2.4 Planner [3, 2, 1], Logistics [5, 3, 0], CustomerService [4, 0, 0]. "now" routingPersona `other` = 0 on every stage (A32, A33 now in their AOF groups). Call lists now include L2 + L3 on every window.
- `raisedToClosed.test.ts`: item calls = population items: 7 d I10 I11 I14 I31 I32 I36 I38 I40; 14 d 11 items; now 15 items; AMER 7 d I31 I32 I36 I38 I40.
- `raisedToFirstView.test.ts`: 7 d 12 items; 30 d 20 items (24-alert population); now 28 items; AMER unchanged.
- `firstViewToClosure.test.ts`: 30 d 8 items; AMER 7 d 3 items.
- Every value matched the code on the first run.

## Edits to tests of files other agents own
None outside my folders. (All tests I changed mirror my files: compute/, loaders/, query/, breakdowns, catalogue, window.)

## New QUESTIONS.md entries (for G5)
- Assumed (COR-01; spec §9 2.1 alert view, §8, §1; Excel 2.2 = 2.1 ∩ …): alert-view 2.2–2.4, their groups and both outside paths count only alerts in the 2.1 population (open now, or closedAt ≥ window start); under "now" open now only. The literal §9 2.2 plan text ("distinct riskAlertId of viewed rows") is narrower than the nesting; nesting wins. Cost: the alert view always loads L2(selected window) and L3 open alerts (both memoised, L3 shared with 4.5).
- Assumed (COR-02; spec §9 2.1 term b, §9 2.2 "groups from L2 attrs", §12.1): in the alert view an open alert is grouped by its AlertOrderFulfillment row on every stage; a closed alert by its L2 attrs (latest pipeline event); escalated exists only for open alerts.
- Assumed (COR-03; spec §5 B2, §9.0 Truncation): candidate groups with a 0 count on the ranking stage are dropped before top-N (item-view alert dims, userFunnel escalated), so they are not shown and not counted in `truncated.total`.
- Assumed (SPF-01; Appendix A F6, spec §10 FunnelSeries): under the count fallback `FunnelSeries.unit` is `"count"` (the unit the numbers are in), with `value-item-view-only`.
- Assumed (L5; spec §9 4.2 "fetch the population's items"): 4.2–4.4 fetch items only for the population chosen by the same pure function the derive uses; excluded alerts (noRaise / closeBeforeView) are part of the population.
- Assumed (MOD-02; spec §9.0 Truncation): the `truncated` MAX_GROUPS rule runs in derive only; for item-view alert dims it checks the 2.1 list, which has one entry per candidate of the grouped `countOpenAlertsBy` call.
- Assumed (spec §9 4.1 `dateIn`): a verdict date given as a timestamp is compared by its UTC date; an unparsable date is outside every window.

## Interface change requests
- `rawTypes.ts` (frozen), doc comment of `AlertViewRaw` only: it still says "`facts` (L2) only with alertType/routingPersona/priority; `openAlerts` (L3 alerts) only under "now" or with escalated". Since COR-01/COR-02 both are always filled by the loader. Suggested text: "`facts` = L2(selected window) and `openAlerts` = L3 open alerts, always loaded (COR-01/02); nullable only for older raws." No type change.
